info:
  name: Gerar todos os FAQs
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8000/faqs/generate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
