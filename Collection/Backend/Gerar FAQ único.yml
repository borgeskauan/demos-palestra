info:
  name: Gerar FAQ único
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/faqs/generate/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
